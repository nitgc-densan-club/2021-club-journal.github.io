on: [push, pull_request]

jobs:
  lint-changelog:
    name: Lint changelog file
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Lint changelog file
      uses: avtodev/markdown-lint@v1.5
      with:
        args: '*.md'